name: Log in to quay.io/apicurio-ci

runs:
  using: composite
  steps:

    - name: Configure env. variables
      shell: bash
      run: |
        echo "QUAY_TMP_ONLY_USERNAME=apicurio-ci+tmp_only" >> $GITHUB_ENV
        echo "QUAY_TMP_ONLY_PASSWORD=RHIJRDB4TZPMCDKV5FMNW9ZKU5L2FIOQI8130QAZHFVTLHPLNRTZ9RBBVWUCXIVB" >> $GITHUB_ENV

    - name: Log in to quay.io/apicurio-ci
      shell: bash
      run: |
        echo $QUAY_TMP_ONLY_PASSWORD | docker login quay.io -u $QUAY_TMP_ONLY_USERNAME --password-stdin
