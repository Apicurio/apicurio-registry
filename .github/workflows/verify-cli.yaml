name: CLI Verification

on:
  workflow_call:
    inputs:
      image-tag:
        type: string
        required: true
        description: 'Build artifact identifier (commit SHA)'

jobs:
  cli-verify:
    name: CLI (placeholder)
    runs-on: ubuntu-22.04
    timeout-minutes: 5
    steps:
      - name: Placeholder
        run: echo "This is a placeholder workflow. The real implementation is in the PR branch."
