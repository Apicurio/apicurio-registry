<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <parent>
    <groupId>io.apicurio</groupId>
    <artifactId>apicurio-registry</artifactId>
    <version>3.4.0-SNAPSHOT</version>
    <relativePath>../pom.xml</relativePath>
  </parent>

  <!-- Keeps the historical artifactId: distro/docker, integration-tests, docs and downstream users
       consume the runnable server under this name. -->
  <artifactId>apicurio-registry-app</artifactId>
  <packaging>jar</packaging>
  <name>Registry :: App</name>
  <description>
    The runnable registry server: packages core (and, by default, the agents module) with Quarkus.
    Contains no code of its own.
  </description>

  <properties>
    <projectRoot>${project.basedir}/..</projectRoot>
    <maven.compiler.release>21</maven.compiler.release>
  </properties>

  <dependencies>
    <dependency>
      <groupId>io.apicurio</groupId>
      <artifactId>apicurio-registry-core</artifactId>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <plugin>
        <groupId>io.quarkus</groupId>
        <artifactId>quarkus-maven-plugin</artifactId>
        <executions>
          <execution>
            <goals>
              <goal>build</goal>
            </goals>
          </execution>
          <!-- Second build producing a Quarkus "mutable-jar" (re-augmentable) distribution in
               target/mutable/quarkus-app. It is the basis of the "-mutable" container image, which
               lets deployers add Java providers for custom artifact types (jars dropped into
               quarkus-app/providers) without rebuilding the registry. Enabled by -Dfull.
               NOTE: do not set quarkus.package.output-directory here - plugin properties are recorded
               in quarkus/build-system.properties and replayed at re-augmentation time. -->
          <execution>
            <id>build-mutable</id>
            <goals>
              <goal>build</goal>
            </goals>
            <configuration>
              <skip>${apicurio.mutable-jar.skip}</skip>
              <bootstrapId>mutable</bootstrapId>
              <buildDir>${project.build.directory}/mutable</buildDir>
              <properties>
                <quarkus.package.jar.type>mutable-jar</quarkus.package.jar.type>
                <quarkus.package.jar.user-providers-directory>providers</quarkus.package.jar.user-providers-directory>
              </properties>
            </configuration>
          </execution>
        </executions>
      </plugin>

      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-assembly-plugin</artifactId>
        <executions>
          <execution>
            <id>assembly</id>
            <goals>
              <goal>single</goal>
            </goals>
            <phase>package</phase>
            <configuration>
              <finalName>${project.artifactId}-${project.version}</finalName>
              <attach>true</attach>
              <descriptors>
                <descriptor>src/main/assembly/assembly.xml</descriptor>
              </descriptors>
              <archiverConfig>
                <defaultDirectoryMode>0755</defaultDirectoryMode>
              </archiverConfig>
              <tarLongFileMode>${tar.long.file.mode}</tarLongFileMode>
            </configuration>
          </execution>
          <!-- Tarball of the mutable-jar distribution (see build-mutable above); not attached/deployed -->
          <execution>
            <id>assembly-mutable</id>
            <goals>
              <goal>single</goal>
            </goals>
            <phase>package</phase>
            <configuration>
              <skipAssembly>${apicurio.mutable-jar.skip}</skipAssembly>
              <finalName>${project.artifactId}-${project.version}</finalName>
              <attach>false</attach>
              <descriptors>
                <descriptor>src/main/assembly/assembly-mutable.xml</descriptor>
              </descriptors>
              <archiverConfig>
                <defaultDirectoryMode>0755</defaultDirectoryMode>
              </archiverConfig>
              <tarLongFileMode>${tar.long.file.mode}</tarLongFileMode>
            </configuration>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>

  <profiles>
    <!-- The agent registry feature. Included unless -DskipAgents is set. -->
    <profile>
      <id>agents</id>
      <activation>
        <property>
          <name>!skipAgents</name>
        </property>
      </activation>
      <dependencies>
        <dependency>
          <groupId>io.apicurio</groupId>
          <artifactId>apicurio-registry-agents</artifactId>
        </dependency>
      </dependencies>
    </profile>
  </profiles>
</project>
