openapi: 3.0.2
info:
  title: Pet Schema
  version: 1.0.0
paths: {}
components:
  schemas:
    Pet:
      type: object
      required:
        - id
        - name
      properties:
        id:
          type: integer
          format: int64
        name:
          type: string
        tag:
          type: string
        owner:
          $ref: './Owner.yaml#/components/schemas/Owner'
