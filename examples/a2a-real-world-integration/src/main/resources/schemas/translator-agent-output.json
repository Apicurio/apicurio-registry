{
  "$schema": "https://apicurio.io/schemas/model-schema/v1",
  "modelId": "translator-output",
  "provider": "a2a-demo",
  "version": "1.0.0",
  "output": {
    "type": "object",
    "required": ["original_language", "target_language", "translated_response"],
    "properties": {
      "original_language": {
        "type": "string",
        "description": "ISO language code of the source text (e.g., en)"
      },
      "target_language": {
        "type": "string",
        "description": "ISO language code of the target translation (e.g., es)"
      },
      "translated_response": {
        "type": "string",
        "description": "The full translated text"
      },
      "translation_notes": {
        "type": "string",
        "description": "Notes about cultural adaptations or translation decisions"
      },
      "confidence": {
        "type": "number",
        "minimum": 0,
        "maximum": 1,
        "description": "Confidence score for the translation quality"
      }
    }
  },
  "metadata": {
    "description": "Output schema for translation agent - translates customer service communications preserving tone",
    "category": "customer-support",
    "tags": ["translation", "localization", "multilingual"],
    "dependsOn": ["llm-agents.schemas/response-generator-output"]
  }
}
