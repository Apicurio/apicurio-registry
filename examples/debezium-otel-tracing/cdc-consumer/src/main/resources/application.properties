# CDC Consumer Configuration
quarkus.application.name=cdc-consumer

# Kafka Configuration
kafka.bootstrap.servers=localhost:9092

# Apicurio Registry Configuration
# Using v3 API with the v3 SerDe library
apicurio.registry.url=http://localhost:8086/apis/registry/v3

# Apicurio Registry base URL (for REST client - path is in @Path annotation)
apicurio.registry.base.url=http://localhost:8086

# OpenTelemetry Configuration
quarkus.otel.traces.enabled=true
quarkus.otel.metrics.enabled=false
quarkus.otel.logs.enabled=false
quarkus.otel.exporter.otlp.endpoint=http://localhost:4317
quarkus.otel.exporter.otlp.protocol=grpc
quarkus.otel.service.name=cdc-consumer
quarkus.otel.traces.sampler=parentbased_always_on

# Apicurio Registry REST Client (for traced API calls)
# Using base URL since @Path annotation includes /apis/registry/v3
quarkus.rest-client.registry-api.url=${apicurio.registry.base.url}

# Logging
quarkus.log.category."io.apicurio".level=INFO
quarkus.log.category."io.opentelemetry".level=WARN
quarkus.log.category."org.apache.kafka".level=WARN
