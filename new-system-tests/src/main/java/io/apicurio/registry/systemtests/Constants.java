package io.apicurio.registry.systemtests;

/**
 * Constants used in test suite and tests.
 */
public class Constants {
    /** Name of ApicurioRegistry instance for tests. */
    public static final String REGISTRY_NAME = "registry";
    /** Name and namespace of PostgreSQL database instance for tests. */
    public static final String POSTGRESQL_NAME = "postgresql";
    /** Name/prefix of Apicurio Registry operator deployment for tests. */
    public static final String REGISTRY_OPERATOR_NAME = "apicurio-registry-operator";
    /** Name of Keycloak deployment for tests. */
    public static final String KEYCLOAK_NAME = "keycloak";
}
