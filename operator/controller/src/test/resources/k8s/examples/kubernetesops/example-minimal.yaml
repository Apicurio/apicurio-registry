apiVersion: registry.apicur.io/v1
kind: ApicurioRegistry3
metadata:
  name: example-kubernetesops-minimal
spec:
  app:
    storage:
      type: kubernetesops
      kubernetesops:
        registryId: my-registry
    ingress:
      host: registry.example.com
  ui:
    ingress:
      host: registry-ui.example.com
