apiVersion: registry.apicur.io/v1
kind: ApicurioRegistry3
metadata:
  name: simple-with-studio-deprecated
spec:
  app:
    # Use `app.ingress.host` since 3.0.7
    host: simple-with-studio-app.apps.cluster.example
  ui:
    # Use `ui.ingress.host` since 3.0.7
    host: simple-with-studio-ui.apps.cluster.example
  studioUi:
    enabled: true
    # Use `studioUi.ingress.host` since 3.0.7
    host: simple-with-studio-studio-ui.apps.cluster.example
