{
  "tests": [
    {
      "enabled": false,
      "id": "Removing optional field should be backwards and forwards compatible, Issue #1710",
      "compatibility": "both",
      "original": {
        "title": "Person",
        "type": "object",
        "properties": {
          "firstName": {
            "type": "string",
            "description": "The person's first name."
          },
          "lastName": {
            "type": "string",
            "description": "The person's last name."
          },
          "age": {
            "description": "Age in years which must be equal to or greater than zero.",
            "type": "integer",
            "minimum": 0
          }
        }
      },
      "updated": {
        "title": "Person",
        "type": "object",
        "properties": {
          "firstName": {
            "type": "string",
            "description": "The person's first name."
          },
          "age": {
            "description": "Age in years which must be equal to or greater than zero.",
            "type": "integer",
            "minimum": 0
          }
        }
      }
    },
    {
      "enabled": true,
      "id": "Adding optional field should be backwards and forwards compatible, Issue #1710",
      "compatibility": "both",
      "original": {
        "title": "Person",
        "type": "object",
        "properties": {
          "firstName": {
            "type": "string",
            "description": "The person's first name."
          },
          "age": {
            "description": "Age in years which must be equal to or greater than zero.",
            "type": "integer",
            "minimum": 0
          }
        }
      },
      "updated": {
        "title": "Person",
        "type": "object",
        "properties": {
          "firstName": {
            "type": "string",
            "description": "The person's first name."
          },
          "lastName": {
            "type": "string",
            "description": "The person's last name."
          },
          "age": {
            "description": "Age in years which must be equal to or greater than zero.",
            "type": "integer",
            "minimum": 0
          }
        }
      }
    },
    {
      "enabled": false,
      "id": "Removing required field should not be backwards compatible, Issue #1710",
      "compatibility": "forward",
      "original": {
        "title": "Person",
        "type": "object",
        "required": ["firstName"],
        "properties": {
          "firstName": {
            "type": "string",
            "description": "The person's first name."
          },
          "lastName": {
            "type": "string",
            "description": "The person's last name."
          },
          "age": {
            "description": "Age in years which must be equal to or greater than zero.",
            "type": "integer",
            "minimum": 0
          }
        }
      },
      "updated": {
        "title": "Person",
        "type": "object",
        "required": ["firstName"],
        "properties": {
          "age": {
            "description": "Age in years which must be equal to or greater than zero.",
            "type": "integer",
            "minimum": 0
          },
          "lastName": {
            "type": "string",
            "description": "The person's last name."
          }
        }
      }
    }
 ]
}
